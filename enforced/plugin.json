{
  "name": "lazyagentic-enforced",
  "version": "1.0.0",
  "rulesVersion": "3.28.0",
  "description": "Enforced split of lazyagentic — hooks + MCP lint for Korean prose (runs alongside rules-only body).",
  "hooks": "./hooks.json",
  "mcpServers": "./mcp_config.json"
}
