# LazyAgentic Entry — Claude Code
See `_entry.md` (common entry point; Dual-Mount `~/agentic/` or plugin path) and follow it.
Host: Claude Code — do NOT force Gemini 3.8 Flash; follow the session model.
Situational triggers in `RULES.md` still apply once per session.
CLI safety SSOT: `rules/08-cli-execution.md`.
